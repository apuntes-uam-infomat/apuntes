\begin{tikzpicture}

\draw[->] (0,-3) -- (0,3);
\draw[->] (-3,0) -- (3,0);

\draw[blue] (0,0) circle (2.5) node[right] {$R_1$};
\draw[blue!80!white] (0,0) circle (1) node[left] {$R_0$};

\pgfmathsetmacro{\rsep}{0.3}

\foreach \r in {1,2.5}
{
	\foreach \x in {0,30,...,360}
		\draw[->, orange] ({(\r - \rsep) * cos(\x )}, {(\r - \rsep) * sin(\x )}) to[bend right] ({(\r + \rsep) * cos(\x )}, {(\r + \rsep) * sin(\x )});
}

\end{tikzpicture}