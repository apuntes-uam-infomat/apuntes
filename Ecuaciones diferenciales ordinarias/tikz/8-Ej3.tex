\begin{tikzpicture}
\draw[->] (-2,0) -- (2,0);
\draw[->] (0,-1.3) -- (0,1.3);

\foreach \d in {90,270}
{
	\foreach \s in {0, 180}
	{
		\draw[rounded corners=15, red, dense directed] ({cos(\s)}, {sin(\s)}) |- ({cos(\d)}, {sin(\d)});
	}
}

\foreach \a in {0,90,180,270}
{
	\node[circle, draw, fill, green, inner sep=1.5pt] at ({cos(\a)}, {sin(\a)}) {};
}

\end{tikzpicture}