\begin{tikzpicture}

\draw[variable=\x, domain=0:2*pi] plot ({\x}, {sin(\x r) / 3});

\foreach \x in {0,0.5,...,6}
{
	\curveNormal[dashed, blue!50!black, length=0.7cm]{sin((\x) r) / 3}
}

\end{tikzpicture}